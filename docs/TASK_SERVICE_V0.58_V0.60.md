# v0.58-v0.60 Execution Closure

这三个小版本完成执行闭环的三块基础：

- 连接器/外部调用：结果回到 TaskService，记录错误类别、验证状态和幂等结果。
- 人工授权：任务可以明确进入 `awaiting_user`，授权通过或拒绝都会写入审计。
- 可观测性：任务可以查询持续时间、步骤完成率、工具成功率、错误分类、交付物、授权和 token 用量。

指标来自任务账本，不是界面本地计时器或模型自报数字。
