# Hermes 主动协作办公室 · 项目开发全记录

> 版本：**v0.1.7**（截至 2026-07-22）
> 定位：Electron 原生桌面应用，把"OPC 四角色 AI 智能体团队"（PM / Planner / Coder / Checker）做成一个可视化协作办公室，支持私聊、群聊团队讨论、任务推进、多模态附件、产出物预览、Token 统计。
> 本文档为**全量开发资料**，覆盖技术栈、依赖、目录结构、架构、版本演进、构建打包、约定与已知坑。本文档取代了早期过时的 `../DEVELOPMENT.md`（仅到 v0.1.1）。

---

## 1. 技术栈与依赖（精确版本）

### 1.1 运行时 / 构建链

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 桌面壳 | Electron | ^33.4.11 | 主进程 `electron/main.cjs`，预加载 `electron/preload.cjs` |
| 打包 | electron-builder | ^25.1.8 | NSIS 安装包，输出到 `release/` |
| 自动更新 | electron-updater | ^6.5.0 | 已接 `autoUpdate.cjs`，但 publish URL 是占位符（见 §8） |
| 日志 | electron-log | ^5.4.4 | 主进程日志 |
| 前端框架 | React | ^19.2.7 | 函数组件 + Hooks |
| 渲染 | react-dom | ^19.2.7 | |
| 构建 | Vite | ^8.1.1 | `vite build` 产出 `dist/` |
| 插件 | @vitejs/plugin-react | ^6.0.3 | |
| 语言 | TypeScript | ~6.0.2 | `tsc -b` 类型检查 |
| 校验 | oxlint | ^1.71.0 | `npm run lint`（非阻塞，构建不走它） |
| 类型 | @types/node / @types/react / @types/react-dom | ^24 / ^19.2 / ^19.2 | |

> ⚠️ 项目开启 `verbatimModuleSyntax` + `noUnusedLocals` + `noUnusedParameters`（见 tsconfig），**跨文件类型导入必须加 `type` 关键字**，未使用的 import/参数会直接编译报错。

### 1.2 外部依赖（无第三方 UI 库）

项目**不依赖**任何 UI 组件库、状态管理库（Redux 等）、拖拽库、图表库——全部手写 CSS + React 内置 `useReducer`。图表（Token 趋势/模型占比）用原生 SVG 绘制。这保证了产物精简、可控。

### 1.3 数据持久化

全部用 **localStorage**（Electron 渲染进程即 Chromium localStorage，跟本机本用户走，不跨机器）：

| Key | 内容 |
|-----|------|
| `hermes_office_state` | 主状态（员工/团队/任务/消息/设置/网络） |
| `hermes_office_settings` | 模型配置 `AppSettings` |
| `hermes_office_chat_<id>` | 群聊消息 |
| `hermes_office_dm_<empId>` | 私聊消息 |
| `hermes_office_user_profile` | 用户画像文本 |
| `hermes_office_user_memory` | 长期记忆条目数组 |
| `hermes_office_tokenlog` | Token 消耗日志（上限 2000） |
| `hermes_office_outputs` | 产出物记录数组 |

---

## 2. 目录结构与文件职责

```
hermes-office-pro/
├── package.json              # 版本/依赖/脚本/electron-builder 配置
├── index.html                # Vite 入口 HTML
├── vite.config.ts
├── tsconfig.json / tsconfig.app.json / tsconfig.node.json
├── electron/
│   ├── main.cjs              # 主进程：窗口管理 + IPC（含 win:openChat / win:broadcast）
│   ├── preload.cjs           # 上下文隔离桥：暴露 electronAPI（含 openChat/broadcast/onBroadcast）
│   └── autoUpdate.cjs        # 自动更新逻辑（electron-updater）
├── src/
│   ├── main.tsx              # React 渲染入口 + 注册 onBroadcast 监听
│   ├── App.tsx               # 顶栏 + 视图路由（#chat 走 ChatOnlyView，否则办公室/数据分析）
│   ├── store.tsx             # 全局状态（useReducer + 包装 dispatch 广播）
│   ├── types.ts              # 全部类型定义（Employee/Team/Task/ChatMessage/Attachment/AppSettings…）
│   ├── electron.d.ts         # window.electronAPI 类型声明
│   ├── ipcBus.ts             # ★ 跨窗口广播总线（v0.1.7 新增）
│   ├── theme.css             # 全部样式（≈12KB，Stripe 风浅色主题）
│   ├── data/
│   │   ├── hermesClient.ts   # ★ 数据层：localStorage CRUD + LLM API（chatCompletion）+ Token 记账
│   │   ├── outputs.ts        # 产出物管理（addOutput/loadOutputs/openOutputInBrowser）+ 广播
│   │   ├── avatarPresets.tsx # 10 张 AI 头像 + 渲染
│   │   ├── defaultEmployees.ts / defaultTeams.ts  # 种子数据
│   ├── engine/
│   │   ├── teamDiscussion.ts # ★ OPC 团队讨论引擎（PM→Planner→Coder→Checker 轮询）
│   │   ├── simulationEngine.ts # 早期演示剧本（已基本弃用，保留兜底）
│   │   ├── proactiveScript.ts  # 主动协作脚本
│   │   └── tools.ts          # Agent 工具（write_file/run_command 等，产出物带 scope）
│   ├── components/
│   │   ├── chat/
│   │   │   ├── ChatOnlyView.tsx  # ★ 原生子窗口容器（解析 hash，渲染对应聊天 + 自绘标题栏）
│   │   │   ├── DmChatApp.tsx      # 私聊（按员工独立模型 + 附件）
│   │   │   ├── TeamChatApp.tsx    # 群聊团队讨论（OPC 流程）
│   │   │   ├── AssistantChat.tsx  # 通用助手（全局模型）
│   │   │   ├── ModelSelector.tsx  # 对话内模型切换器
│   │   │   └── FloatWindowLayer.tsx # 早期 DOM 浮窗层（v0.1.6 起已不再使用，保留文件）
│   │   ├── office/
│   │   │   ├── OfficeView.tsx / Workstation.tsx / AgentAvatar.tsx / OfficeDecor.tsx  # 办公室视觉（头像卡网格）
│   │   ├── outputs/
│   │   │   ├── ChatOutputsPanel.tsx # 对话内产出物面板（按 scope 过滤 + 实时刷新）
│   │   │   ├── OutputRenderer.tsx   # 多类型预览（md/html iframe/json/csv/code/image/url/text）
│   │   │   └── OutputsView.tsx      # 全量产出物视图
│   │   ├── sidebar/
│   │   │   ├── SidebarPanel.tsx / EmployeeCard.tsx / TeamList.tsx
│   │   │   ├── AddEmployeeModal.tsx / EditEmployeeModal.tsx / CreateTeamModal.tsx
│   │   ├── settings/
│   │   │   └── SettingsModal.tsx    # 模型配置 / 用户画像 / 长期记忆 三标签
│   │   ├── analytics/
│   │   │   └── Analytics.tsx        # Token 统计看板（趋势柱状图 + 模型占比环图 + 高耗会话表）
│   │   └── avatar 资源见 src/assets/avatars/*
│   └── utils/
│       ├── attachments.ts   # ★ 附件处理（图片→dataURL / 文本→读内容 / 其他→产出物）
│       ├── linkify.tsx      # URL 转可点击 <a>
│       └── clipboard.ts     # 剪贴板工具
```

★ = 核心文件，改之前务必先通读。

---

## 3. 核心架构

### 3.1 启动链

```
electron/main.cjs（创建主 BrowserWindow，loadFile index.html）
   └─ preload.cjs（contextIsolation=true, nodeIntegration=false，挂载 window.electronAPI）
        └─ src/main.tsx（ReactDOM 渲染 <StoreProvider><App/></StoreProvider>）
             ├─ 注册 window.electronAPI.onBroadcast(...) → deliverBus 分发到本地总线
             └─ src/App.tsx
                  ├─ hash 命中 #chat?type=&id= → 渲染 <ChatOnlyView/>（原生子窗口内容）
                  └─ 否则渲染顶栏 + 办公室视图 / 数据分析视图
```

### 3.2 窗口模型（v0.1.6 起）

- **主窗口**：办公室 + 数据分析（hash 为空或 `#office`/`#analytics`）。
- **聊天子窗口**：真实原生 `BrowserWindow`（无边框 `frame:false`），由 `main.cjs` 的 `win:openChat` 创建。因为不设 parent，可**在屏幕上自由拖动、拖出主窗口**。
- **路由**：子窗口加载同一份 `index.html`，靠 `location.hash = #chat?type=dm|team|assistant&id=xxx` 区分；`ChatOnlyView` 解析 hash 渲染对应聊天组件，并用 CSS `-webkit-app-region: drag` 画一个可拖拽标题栏（含关闭/最小化按钮）。
- **主窗口关闭时**，所有聊天子窗口一并关闭。

### 3.3 状态管理与跨窗口同步（v0.1.7 关键）

- 每个窗口（主 + 各子窗口）是**独立的 React `useReducer` store 实例**。
- `store.tsx` 把原始 `dispatch` 重命名为 `rawDispatch`，再包一层 `dispatch`：
  1. 本地先 `rawDispatch(action)`；
  2. 再用 `sendBus('store:action', action)` 通过 IPC 广播给**其他窗口**。
- 其他窗口收到 `store:action` 后，用 `rawDispatch` 应用，并用 `applyingRemote` ref 防回环（避免收到后再广播回去）。
- `SKIP_BROADCAST` 集合里的 action（如 `INIT`、旧版 DOM 浮窗的 `OPEN_WIN`/`MOVE_WIN` 等窗口管理类）**不广播**。
- 同步范围：**团队消息 / 任务 / 进度 / 员工 / 团队结构**实时一致；**DM 私聊仍是各子窗口本地 state，不跨窗口同步**（已知边界）。

### 3.4 IPC 广播层（v0.1.7 新增 `src/ipcBus.ts`）

- 三层：
  1. `main.cjs` 的 `win:broadcast` IPC：把消息转发给 `BrowserWindow.getAllWindows()` 中**除发送者外**的所有窗口（`webContents.send`）。
  2. `preload.cjs` 暴露 `broadcast(channel,payload)` 和 `onBroadcast(cb)→unsub`。
  3. `src/ipcBus.ts` 的 `sendBus(channel,payload)` / `onBus(channel,cb)` / `deliverBus(channel,payload)` 预定义了两个频道：
     - `store:action` —— store action 跨窗口同步（见 3.3）。
     - `outputs:changed` —— 产出物变更后 `addOutput` 广播，`ChatOutputsPanel` 订阅即时刷新（原 3s 轮询保留作兜底）。

> 已知 bug 修复：preload.cjs 曾有一处语法错误 `openChat: ... opts),),`（多余括号+逗号），会让整个 preload 加载失败、electronAPI 完全不挂载。v0.1.7 已修。

### 3.5 多模态附件（v0.1.6）

- `ChatTurn.content` 支持 `string | ContentPart[]`（ContentPart = `{type:'text',text} | {type:'image_url',image_url:{url}}`）。
- `chatCompletion` / `runAgentLoop` 接受 `attachments?: Attachment[]`：把最后一条 user 消息转成多模态数组。
- 图片 → base64（vision）、文本文件 → 读内容拼进上下文、其他文件 → 保存为产出物。
- 前端：`DmChatApp/TeamChatApp/AssistantChat` 加了 📎 按钮、textarea `onPaste` 粘贴图片、附件 chip 预览/移除。`src/utils/attachments.ts` 负责分类与读取。

### 3.6 产出物系统（v0.1.3 起）

- `OutputRecord`：`{ id, name, path?, contentType, scope, content, createdAt, size? }`。
- `contentType` 自动检测（`contentTypeFromFilename`）：markdown / html / code / json / csv / image / url / text。
- `scope` 过滤：`dm:<empId>` / `team:<teamId>` / `assistant` / `global`。
- `OutputRenderer`：按类型渲染——md 渲染、html 用 `<iframe sandbox="allow-scripts">`（不直接注入 DOM，避免撑开 UI）、code 语法高亮、json 格式化树、csv 表格、image `<img>`、url 可点击。
- `openOutputInBrowser()`：把内容渲染后生成 blob URL 用 `window.open` 在新标签页全屏预览（每项 🌐 按钮）。

---

## 4. API / 数据层（`src/data/hermesClient.ts`）

### 4.1 服务商预设（PROVIDER_PRESETS）

每个含完整 `baseUrl` + `defaultModel`（国内厂商 base_url **本身含版本路径**，不能硬拼 `/v1/`）：

| key | 名称 | baseUrl | 默认模型 | 需 Key |
|-----|------|---------|----------|--------|
| deepseek | DeepSeek | https://api.deepseek.com | deepseek-chat | 是 |
| qwen | 通义千问 | https://dashscope.aliyuncs.com/compatible-mode/v1 | qwen-plus | 是 |
| zhipu | 智谱 GLM | https://open.bigmodel.cn/api/paas/v4 | glm-4-flash | 是 |
| kimi | Kimi | https://api.moonshot.cn/v1 | moonshot-v1-8k | 是 |
| doubao | 豆包 | https://ark.cn-beijing.volces.com/api/v3 | doubao-1.5-pro | 是 |
| hunyuan | 腾讯混元 | https://api.hunyuan.cloud.tencent.com/v1 | hunyuan-pro | 是 |
| openai | OpenAI | https://api.openai.com/v1 | gpt-4o-mini | 是（需代理） |
| ollama | Ollama 本地 | http://localhost:11434/v1 | qwen2.5 | 否 |
| custom | 自定义 | 用户填 | 用户填 | 视情况 |

### 4.2 关键函数

- `loadSettings()/saveSettings()` —— `AppSettings`（provider/apiHost/apiKey/model/autoDiscuss）。
- `resolveApiBase(s)` —— 规整 base_url（去尾斜杠）。
- `endpointUrl(base, path)` —— base 已含 `/v\d+` 则直接拼，否则补 `/v1`。**所有请求都走它**，避免国内厂商路径拼错。
- `chatCompletion(turns, scene, label, modelConfig?)` —— 调 `/v1/chat/completions`（temperature 0.7、30s 超时、Bearer），返回 `{ content, usage, model }`；成功自动记账到 tokenlog；失败抛错由调用方回落本地剧本。
- `testConnection()` / `checkBackend()` —— 探测 `/v1/models`，更新 `backendOnline` 状态。
- `resolveChatSettings(empConfig?)` —— 合并员工独立模型配置与全局设置（员工优先）。
- `appendChat/appendDm` —— 按消息 id 去重（v0.1.7 多窗口落盘防护）。
- `extractUserInsights(conversation, source)` —— 对话后自动用 LLM 提炼用户画像/记忆，写入长期记忆。
- `buildUserContext()` —— 拼接用户画像 + 最近记忆，注入 system prompt。

---

## 5. 引擎层

### 5.1 团队讨论引擎（`engine/teamDiscussion.ts`）—— OPC 流程

`runTeamDiscussion` 按 **PM → Planner → Coder → Checker** 顺序轮询调 `chatCompletion`：

- `memberSpeak(role, ctx)`：组装 `ROLE_DUTY` 职责 + 员工 prompt 人设 + 最近 10 条群消息上下文 + 指令，返回 `{ text, tokens }`。
- 成员回复经 `onMessage` 发进群；关联任务随角色推进 `PLANNING → CODING → REVIEW`，结尾 PM 验收 → `DONE`。
- 无 API 时用 `FALLBACK_LINES` 兜底；`memberByRole` 找对应成员。
- 触发：发消息后自动（`autoDiscuss` 开）/ 点「发起讨论」按钮（`autoDiscuss` 关）；发任务后 `setTimeout` 自动触发带 task 的讨论。

### 5.2 其他引擎

- `simulationEngine.ts`：早期写死剧本演示，**已基本弃用**，仅作兜底。
- `proactiveScript.ts`：主动协作脚本。
- `tools.ts`：Agent 工具，`write_file` / `run_command` 生成的产出物自动带 `scope`，`contentType` 自动检测。

---

## 6. 版本演进史

| 版本 | 日期 | 核心变更 |
|------|------|----------|
| 0.1.0 | 07-20 | Electron 原生桌面初版（无边框窗口 + 桌面外壳 + 四角色看板） |
| 0.1.1 | 07-21 | Coze×Marvis 风办公室：CSS 2.5D 等距 + 浮动 IM 群聊；修复 13 个死文件、UI 全崩（误引 index.css） |
| 0.1.2 | 07-22 | 浮窗边界 clamp + 置顶 + 文本可选复制 + 右侧产出物面板 + 链接可点击；员工编辑按钮可见性修复；新增 ModelSelector；soul.md 人格/画像/记忆/独立模型全部就绪 |
| 0.1.3 | 07-22 | 产出物分项目展示 + 多类型预览（OutputRenderer/ChatOutputsPanel，按 scope 过滤） |
| 0.1.4 | 07-22 | 删全局产出物面板（避免点产出物改变整体 UI）+ 外部浏览器打开按钮 |
| 0.1.5 | 07-22 | HTML 改 iframe 沙箱（不撑开 UI）；浮窗层全屏覆盖 + clampPos 全屏范围 |
| 0.1.6 | 07-22 | **架构重构**：聊天窗口改原生 BrowserWindow 子窗口（可全屏拖动）+ 文件上传/粘贴 + 多模态附件；移除 DOM 浮窗层 |
| 0.1.7 | 07-22 | **加窗口间 IPC 广播层**（ipcBus + win:broadcast），修复团队子窗口与主办公室不同步；顺手修 preload 语法错误；store dispatch 包装广播；appendChat/appendDm 去重；产出物实时同步 |

---

## 7. 构建与开发命令

```bash
npm run dev          # vite 开发服务器（http://localhost:5173，多实例端口递增）
npm run build        # tsc -b && vite build（类型检查 + 打包到 dist/）
npm run lint         # oxlint（非阻塞）
npm run start        # electron . 直接跑（开发调试原生窗口）
npm run dist:win     # build + electron-builder --win（不发布），产物 release/*.exe
npm run release:win  # build + electron-builder --win --publish=always（需配置更新服务器）
```

构建要求：**0 错误**才允许打包。`electron-builder` 走 npmmirror 镜像；如遇沙箱 `NODE_OPTIONS=--use-system-ca` 致 Electron 进程报错，需 `unset NODE_OPTIONS`。

---

## 8. 配置与约定

### 8.1 软件名称（electron-builder）

- `appId`: `com.hermes.office`（内部，保持不变）
- `productName`: `Hermes 主动协作办公室`（对外展示名）
- `nsis.shortcutName`: 同上

> ⚠️ **改名约定（用户要求，下次打包执行）**：对外名字改为 **「私人办公会所」**。需改 `package.json` 的 `build.productName` / `build.nsis.shortcutName`、`main.cjs` 创建窗口时的 title、`App.tsx` 顶栏显示名（若有硬编码）。内部 `appId` 不动。

### 8.2 分发策略（已定）

- 直接把 `release/Hermes 主动协作办公室 Setup 0.1.7.exe` 发给对方即可——对方装完是**全新空办公室**。
- **不内置 API Key**，不做配置导入/导出；模型配置由对方进「设置→模型配置」自行填写。
- 安装包 **Windows 专属**（nsis target），Mac/Linux 不可用。
- 未代码签名 → Windows SmartScreen 会拦截，需对方点"更多信息→仍要运行"（非病毒）。
- `autoUpdater` 的 publish URL 是占位符 `https://hermes-update.example.com/releases`，**当前不会自动更新**，纯手动发新包。

### 8.3 数据重置

侧栏底部「↺」按钮：清空 `hermes_office*` 全部 localStorage 并 reload，从干净状态开始（调试用）。

---

## 9. 已知限制

1. **DM 私聊不跨窗口同步**：同一员工的私聊若开两个窗口，各自独立，不通过 IPC 广播（团队数据已全量同步）。
2. **无后端服务器**：纯前端直连公网 LLM API，无本地 server；`checkBackend` 仅探测在线状态。
3. **未签名**：SmartScreen 拦截（见 §8.2）。
4. **无自动更新服务器**：publish URL 占位（见 §8.2）。
5. **无 git 远程**：项目已 `git init` 并 commit 到本地 `master` 分支，但**无 origin 远程**，无法 push（需接远程地址自行 push）。
6. **FloatWindowLayer.tsx 已成死文件**（v0.1.6 起不用），保留未删。

---

## 10. 关键坑与避坑清单（高复用价值）

1. **verbatimModuleSyntax + noUnusedLocals**：跨文件类型导入必须 `import type { X }`；未用 import / 未用参数都会编译失败。内联 `import('./types').Foo` 语法在类型位置不被允许，须顶层 `import type`。
2. **含 JSX 的文件必须 .tsx**：`avatarPresets` 含 JSX，原为 `.ts` 导致 TS 把 `<` 当泛型全崩 → 改 `.tsx`。
3. **Electron 打包图标须 .ico**：svg 报 "format not supported"，故 `build.win.icon` 置 `null` 用默认。
4. **沙箱 NODE_OPTIONS**：`NODE_OPTIONS=--use-system-ca` 会让 electron 进程报 not allowed → `unset NODE_OPTIONS`。
5. **modal 点内即关的 bug**：`onClick` 判断写成 `e.target === e.target`（恒真）→ 必须 `e.target === e.currentTarget`（仅点遮罩才关）。
6. **国内 LLM base_url 含路径**：不能硬拼 `/v1/`，统一走 `endpointUrl(base, path)`。
7. **HTML 产出物撑开 UI**：不能 `dangerouslySetInnerHTML`，改 `<iframe sandbox>`。
8. **DOM 浮窗无法离开单 Electron 窗口**：要全屏拖动必须改用原生 `BrowserWindow` 子窗口（无 parent）。
9. **preload 语法错误致 electronAPI 全挂**：`openChat: ... opts),),` 这类多余括号/逗号会让整份 preload 加载失败。
10. **大改前先建 git 基线**：项目曾无 git，回退 QQ 风时只能手动恢复文件；务必先提交再大改。

---

## 11. 快速上手指引（给未来开发者 / AI）

> 若把本文档交给 AI 继续开发，给出以下上下文即可上手：
> 1. 技术栈见 §1；目录与职责见 §2；核心架构见 §3。
> 2. 改 UI 先看 `theme.css`（Stripe 风浅色主题，CSS 变量在 `:root`）。
> 3. 加新聊天能力 → 改 `DmChatApp/TeamChatApp/AssistantChat` 三者之一，注意三者结构一致。
> 4. 接新 LLM 厂商 → 在 `hermesClient.ts` 的 `PROVIDER_PRESETS` 加一项，并确认 `endpointUrl` 处理路径。
> 5. 加跨窗口同步的新状态 → 在 `store.tsx` 确认该 action 不在 `SKIP_BROADCAST`，否则不会广播。
> 6. 改对外名字 → 见 §8.1 改名约定。
> 7. 打包前确认 `npm run build` 0 错误，再 `npm run dist:win`。
> 8. 必读历史坑见 §10。

---

*最后更新：2026-07-22 · v0.1.7 · 覆盖全部 14 轮迭代 + IPC 广播层 + 改名/分发约定*
